<section class='user-info-wrapper'>
  <ul id='user-info'>
    <p><%= user.name %></p>
    <p><%= user.email %></p>
    <p><%= image_tag user.thumbnail %></p>
  </ul>

<section id='Visits'>
  <% user.visited_restaurants.each do |restaurant| %>
    <p class='visit'><%= restaurant.name %></p>
  <% end %>
</section>


<%= button_to 'Log Out', logout_path, method: :get, class: 'button' %>

</section>
