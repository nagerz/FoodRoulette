<h1>Survey results! Watch them roll in...<h1>

<h3>Survey status: <%= facade.status %></h3>

<p>Total Votes Recieved: <%= facade.vote_count %></p>

<% facade.survey_restaurants.each do |survey_restaurant| %>
  <section class='survey-restaurant'>
    <section class='survey-resturant-<%= survey_restaurant.id %>'>
  <p><%= survey_restaurant.restaurant.name %></p>
  <p>Votes recieved: <%= survey_restaurant.votes %>

  <%= button_to 'Vote for this', survey_path(facade.survey), method: :patch,
      params: { phone: "number_1",
                restaurant: restaurant,
               } %>

<% end %>

<%= button_to 'End Survey Now', end_survey_path(facade.survey), method: :post %>
